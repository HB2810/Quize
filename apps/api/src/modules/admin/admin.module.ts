import { Module } from "@nestjs/common";

/**
 * Admin domain: future CMS endpoints for managing journeys, content,
 * versions, and publishing — every mutation audit-logged. Built out in
 * Foundation 13.
 */
@Module({})
export class AdminModule {}
